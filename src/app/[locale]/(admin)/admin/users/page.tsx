import Link from "next/link";
import { getAdminUsers } from "@/server/admin/companies-users";
type Params = Promise<Record<string, string | string[] | undefined>>; const one = (v: string | string[] | undefined) => Array.isArray(v) ? v[0] : v;
export default async function AdminUsersPage({ searchParams }: { searchParams: Params }) { const raw = await searchParams; const filters = Object.fromEntries(Object.entries(raw).map(([k, v]) => [k, one(v)])); const result = await getAdminUsers(filters); return <main className="min-h-[70vh] bg-slate-50 py-10"><div className="mx-auto max-w-6xl px-5 sm:px-8"><Link href="/ru/admin" className="text-sm underline">Администрирование</Link><h1 className="mt-3 text-3xl font-semibold">Пользователи</h1><nav className="mt-4 flex gap-4 text-sm"><Link className="underline" href="/ru/admin/companies">Компании</Link><Link className="underline" href="/ru/admin/users">Пользователи</Link></nav><form method="get" className="mt-6 flex flex-wrap gap-3 rounded-2xl border bg-white p-4"><input name="q" defaultValue={filters.q} maxLength={100} placeholder="Имя или email" className="rounded-lg border px-3 py-2" /><input name="company" defaultValue={filters.company} placeholder="ID компании" className="rounded-lg border px-3 py-2" /><select name="hasDiagnostics" defaultValue={filters.hasDiagnostics ?? ""} className="rounded-lg border px-3 py-2"><option value="">Диагностики: все</option><option value="yes">Есть диагностики</option><option value="no">Нет диагностик</option></select><button className="rounded-lg bg-blue-800 px-4 py-2 font-semibold text-white">Применить</button><Link className="rounded-lg border px-4 py-2" href="/ru/admin/users">Сбросить фильтры</Link></form>{result.rows.length ? <><div className="mt-6 overflow-x-auto rounded-2xl border bg-white"><table className="min-w-[800px] w-full text-left text-sm"><thead className="bg-slate-100"><tr>{["Имя","Email","Компания","Диагностики","Действие"].map((x)=><th key={x} className="px-3 py-3">{x}</th>)}</tr></thead><tbody>{result.rows.map((row)=><tr key={row.id} className="border-t"><td className="px-3 py-3 font-medium">{row.full_name}</td><td className="px-3 py-3">{row.email ?? "Не указано"}</td><td className="px-3 py-3">{row.companyName}</td><td className="px-3 py-3">{row.diagnosticCount}</td><td className="px-3 py-3"><Link className="underline" href={`/ru/admin/users/${row.id}`}>Открыть</Link></td></tr>)}</tbody></table></div><div className="mt-4 flex justify-between text-sm"><span>Страница {result.page} из {result.totalPages} · всего {result.total}</span><span className="flex gap-3">{result.page > 1 && <Link className="underline" href={`/ru/admin/users?page=${result.page - 1}`}>Назад</Link>}{result.page < result.totalPages && <Link className="underline" href={`/ru/admin/users?page=${result.page + 1}`}>Далее</Link>}</span></div></> : <p className="mt-6 rounded-2xl border bg-white p-6">Пользователи не найдены.</p>}</div></main>; }
